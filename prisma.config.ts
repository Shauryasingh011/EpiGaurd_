/**
 * Firebase Configuration
 * 
 * This file is no longer used as we've migrated to Firebase.
 * All database operations now use Firestore instead of Prisma.
 * 
 * See lib/firebase/ directory for Firebase configuration and utilities.
 */

export default {}
